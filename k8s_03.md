1) Создать Pod с образом busybox или alpine, который запускает команду sleep 3600.
2) Создать Pod с двумя переменными окружения (RELEASE и ENV) и вывести их через printenv внутри контейнера.
3) Докеризовать приложение
```python
from http.server import BaseHTTPRequestHandler, HTTPServer
import os

VERSION = os.getenv("APP_VERSION", "1.0")

class Handler(BaseHTTPRequestHandler):
    def do_GET(self):
        if self.path == "/":
            body = f"Hello from app version {VERSION}\n"
            self.send_response(200)
        elif self.path == "/health":
            body = "ok\n"
            self.send_response(200)
        elif self.path == "/version":
            body = f"{VERSION}\n"
            self.send_response(200)
        else:
            body = "not found\n"
            self.send_response(404)

        self.send_header("Content-Type", "text/plain")
        self.end_headers()
        self.wfile.write(body.encode())

HTTPServer(("0.0.0.0", 8080), Handler).serve_forever()
```
4) Создать Deployment для приложения из п3 с 3 репликами и сервис типа ClusterIP
